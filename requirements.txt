# Core dependencies
numpy>=1.21.0
pyyaml>=6.0
python-dotenv>=0.19.0
anthropic>=0.40.0
scikit-learn>=1.0.0

# Optional: dense semantic embeddings (heavy — pulls torch on its own)
# sentence-transformers>=2.2.0

# Development tools
pytest>=7.0.0
# ruff substitui black + flake8 (formatação e lint numa ferramenta só).
ruff>=0.6.0
mypy>=1.11.0
types-PyYAML>=6.0.0
