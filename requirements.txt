# Core dependencies
numpy>=2.5.1
pyyaml>=6.0.3
python-dotenv>=1.2.2
anthropic>=0.120.0
scikit-learn>=1.9.0

# Optional: dense semantic embeddings (heavy — pulls torch on its own)
# sentence-transformers>=2.2.0

# Development tools
pytest>=9.1.1
black>=26.5.1
flake8>=7.3.0
